//Organizar um array de strings

string[] carros = { "Volvo", "BMW", "Ford", "Mazda" };

Array.Sort(carros);

foreach (string i in carros)
{
  Console.WriteLine(i);
}

//Organizar um array de números

int[] numeros = { 5, 1, 8, 9 };

Array.Sort(numeros);

foreach (int i in numeros)
{
  Console.WriteLine(i);
}